def ex1(n):
    """(int) -> none 
you give the number then the function dose the divisions , until reaching 1 then return
the number of steps"""
    m=0
    while n!=1:
        if (n % 2) == 0:
            n= n/2
        else:
            n=(n*3)+1
        m=m+1
    return m
print(ex1(15))
